spring:
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: jpa
  flyway:
    enabled: false

server:
  port: 8080

logging:
  level:
    root: INFO
    pl:
      futurecollars:
        invoicing: DEBUG
